/* Imports de Bibliotecas, Componentes e Estilos */
import { createApp } from 'vue';
import App from './App.vue';
import './style.css';

/* Criando a Aplicação usando o Componente Principal (App.vue) */
createApp(App)
    .mount('#app') /* Montando a Vue Instance */
